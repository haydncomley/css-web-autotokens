export const STYLE_SETTINGS = {
    // The name of the package to find within the node_modules folder.
    packageName: '',
    // The path to the package's CSS file relative to the node_modules folder.
    packageCssReference: '',
    // The prefix used within the CSS file to identify the CSS variables.
    tokenPrefix: '',
};